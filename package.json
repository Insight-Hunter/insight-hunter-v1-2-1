{
  "name": "insight-hunter-v1-2-1",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build && wrangler build",
    "preview": "vite preview --port 5173",
    "dev:worker": "vite build && wrangler dev",
    "tail": "wrangler tail",
    "publish": "wrangler publish",
    "test": "wrangler test",
    "generate": "wrangler generate",
    "kv:namespace": "wrangler kv:namespace",
    "kv:key": "wrangler kv:key",
    "clean:wrangler": "node -e \"try{require('fs').rmSync('.wrangler',{recursive:true,force:true})}catch(_){}\"",                             
    "dev": "vite build --watch & wrangler dev",       
    "deploy": "npm run clean:wrangler && npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "undefined": "^0.1.0",
    "zod": "^4.1.5",
    "zod-validator": "^1.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.4",
    "@types/react-dom": "^18.3.0",
    "hono": "^4.9.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "wrangler": "^3.114.14"
  }
}
