import type { FC } from "react";
export const ReportsPage: FC = () => (
  <main>
    <h1 style={{ fontSize: 28, marginBottom: 8 }}>Reports</h1>
    <p className="sub">Monthly summary · Audit log · Invoice aging · Export PDF/CSV</p>
  </main>
);
