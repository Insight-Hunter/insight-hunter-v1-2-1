import type { FC } from "react";
export const DashboardPage: FC = () => (
  <main>
    <h1 style={{ fontSize: 28, marginBottom: 8 }}>Dashboard</h1>
    <p className="sub">Cash Flow · Invoice Risk · Wallet Sync</p>
  </main>
);
